import React from "react";
import ErrorMessage from "./index";

export default {
  title: "ErrorMessage",
  component: ErrorMessage
};
export const _default = () => <ErrorMessage message={"test"} />;
